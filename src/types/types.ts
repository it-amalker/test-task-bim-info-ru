export type PersonType = {
  id: number;
  name: string;
  age: number;
};

export type CreatePersonType = {
  id: string;
  name: string;
  age: string;
};

export type SortType = {
  sortBy: string;
};
